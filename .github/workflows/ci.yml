name: Python CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      # Checkout repository code
      - name: Checkout code
        uses: actions/checkout@v3

      # Set up Python environment
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      # Install dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # Run tests
      - name: Run tests
        run: |
          python -m unittest discover -s . -p "test_mark_attendance.py"

      # Generate coverage report (optional, requires coverage.py)
      - name: Generate coverage report
        run: |
          pip install coverage
          coverage run -m unittest discover -s . -p "test_mark_attendance.py"
          coverage report

  deploy:
    if: github.ref == 'refs/heads/main' && success()
    runs-on: ubuntu-latest

    steps:
      # Checkout repository code
      - name: Checkout code
        uses: actions/checkout@v3

      # Example deployment step (replace with your own deployment logic)
      - name: Deploy application
        run: echo "Deployment step (replace with actual deployment commands)"
